#include<stdio.h>
#include<conio.h>
void main()
{
int x;
int ret;
ret =eveodd(x);
printf("the number is :",&x);
scanf("%d",x);
return 0;
}
int eneodd(int x)
{
int result;
if(x%2=0);
result=printf("the number %d is even",x);
else
result=printf("t6he number %d is enen",x);
}